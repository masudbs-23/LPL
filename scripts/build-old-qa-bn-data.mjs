/**
 * Builds scripts/old-qa-bn-data.mjs — Bengali Q&A for Old Q&A (89 items).
 * Run: node scripts/build-old-qa-bn-data.mjs
 */
import fs from "fs";
import path from "path";
import { fileURLToPath } from "url";
import { parseSudheerOldEnglish } from "./parse-sudheer-en.mjs";

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const root = path.join(__dirname, "..");

const qBn = [
  "কেন সরাসরি state আপডেট করা উচিত নয়?",
  "setState()-এ callback ফাংশনের উদ্দেশ্য কী?",
  "JSX callback-এ মেথড বা ইভেন্ট হ্যান্ডলার কীভাবে bind করবেন?",
  "ইভেন্ট হ্যান্ডলার বা callback-এ প্যারামিটার কীভাবে পাস করবেন?",
  "refs-এর ব্যবহার কী?",
  "refs কীভাবে তৈরি করবেন?",
  "forward refs কী?",
  "callback refs ও findDOMNode()-এর মধ্যে কোনটি ভালো?",
  "String Refs কেন legacy?",
  "কম্পোনেন্ট lifecycle-এর বিভিন্ন ধাপ কী কী?",
  "React-এর lifecycle methods কী কী?",
  "HOC কম্পোনেন্টে props proxy কীভাবে তৈরি করবেন?",
  "context কী?",
  "super constructor-এ props আর্গুমেন্ট ব্যবহারের উদ্দেশ্য কী?",
  "ডাইনামিক key নাম দিয়ে state কীভাবে সেট করবেন?",
  "রেন্ডারে ফাংশন প্রতিবার কল হওয়ার সাধারণ ভুল কী?",
  "React v16-এ error boundaries কী?",
  "React v15-তে error boundaries কীভাবে handle করা হয়?",
  "react-dom-এর render মেথডের উদ্দেশ্য কী?",
  "constructor-এ setState() ব্যবহার করলে কী হবে?",
  "componentWillMount()-এ setState() ব্যবহার করা ভালো?",
  "initial state-এ props ব্যবহার করলে কী হবে?",
  "React-এ decorators কীভাবে ব্যবহার করবেন?",
  "CRA কী এবং এর সুবিধা কী?",
  "mounting-এ lifecycle methods-এর ক্রম কী?",
  "React v16-এ কোন lifecycle methods deprecated হবে?",
  "getDerivedStateFromProps() lifecycle method-এর উদ্দেশ্য কী?",
  "getSnapshotBeforeUpdate() lifecycle method-এর উদ্দেশ্য কী?",
  "কম্পোনেন্ট নামকরণের recommended উপায় কী?",
  "class component-এ methods-এর recommended order কী?",
  "কেন setState()-এ ফাংশন পাস করতে হয়?",
  "isMounted() কেন anti-pattern এবং সমাধান কী?",
  "constructor ও getInitialState()-এর পার্থক্য কী?",
  "setState ছাড়া re-render চাপানো যায়?",
  "ES6 class-এ super() ও super(props)-এর পার্থক্য কী?",
  "setState() ও replaceState()-এর পার্থক্য কী?",
  "state পরিবর্তন কীভাবে শুনবেন?",
  "React state থেকে array element সরানোর recommended উপায় কী?",
  "HTML রেন্ডার ছাড়া React ব্যবহার করা যায়?",
  "state-এ object আপডেটের উপায় কী কী?",
  "create-react-app-এ polyfills যোগ করার উপায় কী?",
  "create-react-app-এ https কীভাবে ব্যবহার করবেন?",
  "create-react-app-এ relative import এড়ানোর উপায় কী?",
  "প্রতি সেকেন্ডে কম্পোনেন্ট আপডেট কীভাবে করবেন?",
  "constructor কেন শুধু একবার কল হয়?",
  "React-এ constants কীভাবে define করবেন?",
  "React-এ programmatically click event কীভাবে trigger করবেন?",
  "কোন lifecycle-এ AJAX call করবেন?",
  "render props কী?",
  "load-এ action কীভাবে dispatch করবেন?",
  "React Redux-এ connect() কীভাবে ব্যবহার করবেন?",
  "Redux connect decorator-এ @ symbol-এর উদ্দেশ্য কী?",
  "create-react-app-এ TypeScript কীভাবে ব্যবহার করবেন?",
  "ES6 class-এ statics object কাজ করে?",
  "inline ref callbacks কেন recommended নয়?",
  "HOC factory implementations কী?",
  "class field declarations syntax কীভাবে ব্যবহার করবেন?",
  "event handlers-এ error boundaries দরকার নেই কেন?",
  "try/catch ও error boundaries-এর পার্থক্য কী?",
  "class component-এ required method কোনটি?",
  "render method-এর সম্ভাব্য return types কী?",
  "constructor-এর মূল উদ্দেশ্য কী?",
  "React component-এ constructor বাধ্যতামূলক?",
  "componentWillUnmount-এ setState কেন ডাকবেন না?",
  "getDerivedStateFromError-এর উদ্দেশ্য কী?",
  "re-render-এ methods-এর order কী?",
  "error handling-এ কোন methods invoke হয়?",
  "unmountComponentAtNode method-এর উদ্দেশ্য কী?",
  "HOCs-এর সীমাবদ্ধতা কী?",
  "DevTools-এ forwardRefs কীভাবে debug করবেন?",
  "render methods-এ arrow functions ব্যবহার করা ভালো?",
  "state updates merged হয় বলতে কী বোঝায়?",
  "event handler-এ arguments কীভাবে পাস করবেন?",
  "কম্পোনেন্ট রেন্ডার প্রতিরোধ কীভাবে করবেন?",
  "context ব্যবহারের উদাহরণ দিন?",
  "contextType কীভাবে ব্যবহার করবেন?",
  "consumer কী?",
  "context-এ performance corner cases কীভাবে সমাধান করবেন?",
  "HOCs-এ forward ref-এর উদ্দেশ্য কী?",
  "সব function/class component-এ ref argument পাওয়া যায়?",
  "forward refs-এ component library-তে extra care কেন?",
  "ES6 ছাড়া React class component কীভাবে তৈরি করবেন?",
  "JSX ছাড়া React ব্যবহার করা যায়?",
  "render props দিয়ে HOC কীভাবে তৈরি করবেন?",
  "react-scripts কী?",
  "create react app-এর features কী?",
  "renderToNodeStream method-এর উদ্দেশ্য কী?",
  "create-react-app-এ redux scaffolding কীভাবে পাবেন?",
  "state mutation কী এবং কীভাবে প্রতিরোধ করবেন?",
];

/** Curated Bengali answers (aligned with sudheer Old Q&A). */
const aBn = [
  "সরাসরি `this.state.message = 'Hello'` লিখলে React re-render ট্রিগার করে না। `this.setState({ message: 'Hello' })` ব্যবহার করুন—এটি আপডেট শিডিউল করে। constructor বা class field syntax-এ সরাসরি assign করা যায়।",
  "setState অ্যাসিঙ্ক্রোনাস; আপডেটের ঠিক পরেই নতুন state নির্ভর কাজের জন্য দ্বিতীয় আর্গুমেন্ট হিসেবে callback দিন। ফাংশন কম্পোনেন্টে `useEffect` দিয়ে একই ধরনের কাজ করা যায়।",
  "ক্লাস কম্পোনেন্টে তিন উপায়: (১) constructor-এ `.bind(this)`, (২) public class fields (`handleClick = () => {}`), (৩) JSX-এ arrow `onClick={() => this.handleClick()}`। child-এ prop হিসেবে পাস করলে arrow বেশি re-render করতে পারে—bind/fields ভালো।",
  "`<button onClick={() => this.handleClick(id)} />` অথবা `onClick={this.handleClick.bind(this, id)}`। curried arrow: `handleClick = (id) => () => { ... }`।",
  "ref DOM বা ক্লাস ইনস্ট্যান্সে সরাসরি অ্যাক্সেস দেয়। বেশিরভাগ ক্ষেত্রে এড়িয়ে চলুন; focus, scroll, third-party ইন্টিগ্রেশনে দরকার হতে পারে।",
  "`React.createRef()` + `ref={this.myRef}` অথবা callback ref `(el) => { this.txt = el }`। ফাংশন কম্পোনেন্টে `useRef`।",
  "`React.forwardRef((props, ref) => <button ref={ref} />)` child-এ ref পাঠায়।",
  "callback refs বা `createRef` ব্যবহার করুন; `findDOMNode()` এড়ান—ভবিষ্যৎ React অপ্টিমাইজেশনে বাধা দেয়।",
  "String refs (`ref='x'`, `this.refs.x`) legacy: React stateful রাখে, composable নয়, static analysis-এ খারাপ; React 16-এ সরানো হয়েছে।",
  "তিন ধাপ: Mounting (constructor → getDerivedStateFromProps → render → componentDidMount), Updating (shouldComponentUpdate, render, getSnapshotBeforeUpdate, componentDidUpdate), Unmounting (componentWillUnmount)। React-এর Render/Pre-commit/Commit ফেজও আছে।",
  "16.3-এর আগে: componentWillMount, componentDidMount, componentWillReceiveProps, shouldComponentUpdate, componentWillUpdate, componentDidUpdate, componentWillUnmount। 16.3+: getDerivedStateFromProps, componentDidMount, shouldComponentUpdate, getSnapshotBeforeUpdate, componentDidUpdate, componentWillUnmount।",
  "HOC render-এ নতুন props মার্জ করে wrapped কম্পোনেন্টে পাঠান: `<WrappedComponent {...this.props} {...newProps} />`।",
  "Context ট্রিতে props ছাড়াই ডেটা শেয়ার করে (theme, auth, locale)। `React.createContext(defaultValue)` → Provider/Consumer।",
  "child constructor-এ `super(props)` না হলে `this.props` undefined থাকে; constructor-এর বাইরে পার্থক্য নেই। `props` প্যারামিটার সবসময় পাওয়া যায়।",
  "computed property: `this.setState({ [event.target.id]: event.target.value })`।",
  "ভুল: `onClick={this.handleClick()}` (প্রতি render-এ কল)। সঠিক: `onClick={this.handleClick}` বা arrow wrapper।",
  "Error boundary child tree-এর JS error ধরে fallback UI দেখায়। `componentDidCatch` বা `static getDerivedStateFromError` সহ class component।",
  "React 15-এ `unstable_handleError`; v16-এ `componentDidCatch`।",
  "`ReactDOM.render(element, container, callback)` DOM-এ রেন্ডার/আপডেট করে; optional callback render/update-এর পর চলে।",
  "constructor-এ setState re-render চায় কিন্তু কম্পোনেন্ট এখনো mount হয়নি—তাই `this.state = {...}` দিয়ে initialize করুন।",
  "componentWillMount-এ setState নিরাপদ কিন্তু async/side-effect এড়ান; ডেটা fetch `componentDidMount`-এ করুন।",
  "constructor-এ props থেকে state একবারই সেট হয়; props পরে বদলালে state আপডেট হয় না—controlled হলে render-এ `this.props` ব্যবহার করুন।",
  "Decorator HOC-এর syntactic sugar (`@setTitle('Profile')`)—class wrap করে `componentDidMount`-এ title ইত্যাদি সেট করে।",
  "Create React App: zero-config React, JSX, ES6, test runner, dev server, production build। `npx create-react-app my-app`।",
  "Mounting ক্রম: constructor() → static getDerivedStateFromProps() → render() → componentDidMount()।",
  "componentWillMount, componentWillReceiveProps, componentWillUpdate unsafe; 16.3+ থেকে `UNSAFE_` prefix, v17-এ সরানোর পরিকল্পনা।",
  "render-এর আগে props থেকে state derive; object return করলে state আপডেট, null মানে আপডেট দরকার নেই। componentWillReceiveProps-এর বিকল্প।",
  "DOM commit-এর ঠিক আগে snapshot (scroll position); মান componentDidUpdate-এর তৃতীয় আর্গুমেন্ট।",
  "displayName-এর বদলে class/function reference দিয়ে নাম দিন (`export default class TodoApp`)।",
  "ক্রম: static → constructor → lifecycle (mount/update/unmount) → handlers → getters → optional render helpers → render()।",
  "setState batch করে; একাধিক `setState({ count: this.state.count+1 })` একবারই যোগ হতে পারে। ফাংশন ফর্ম: `setState((prev, props) => ({ count: prev.count + 1 }))`।",
  "isMounted() warning লুকায় কিন্তু unmount-পর setState ঠিক করে না। callback cancel করুন `componentWillUnmount`-এ।",
  "ES6 class: constructor-এ `this.state`। `React.createClass`: `getInitialState()` (deprecated v16+)।",
  "`forceUpdate(callback)` re-render চাপায়; props/state ছাড়া ডেটা থাকলে—তবু এড়িয়ে চলুন।",
  "constructor-এ `this.props` চাইলে `super(props)`; শুধু `super()` হলে constructor-এ `this.props` undefined।",
  "setState merge করে; replaceState পুরো state প্রতিস্থাপন করে (বিরল)।",
  "`componentDidUpdate(prevProps, prevState)`-এ তুলনা করে side-effect চালান।",
  "`filter` দিয়ে নতুন array: `this.setState({ data: this.state.data.filter((_, i) => i !== index) })`—মিউটেট করবেন না।",
  "হ্যাঁ: `return null`, `false`, `[]` (16+), `Fragment` (16.2+), `undefined` (18+)।",
  "Object.assign বা spread দিয়ে copy, তারপর setState; বা functional setState: `setState(prev => ({ user: { ...prev.user, age: 42 } }))`।",
  "core-js manual import অথবা polyfill.io CDN `index.html`-এ।",
  "HTTPS=true env: package.json start script-এ set HTTPS=true (Windows) বা HTTPS=true npm start।",
  "root `.env`-এ `NODE_PATH=src/app`—dev server restart।",
  "`componentDidMount`-এ `setInterval`, `componentWillUnmount`-এ `clearInterval`।",
  "reconciliation একই জায়গায় একই টাইপের কম্পোনেন্টকে একই instance ধরে—constructor আবার চলে না।",
  "static class field: `static DEFAULT_PAGINATION = 10;`।",
  "ref সংরক্ষণ করে `this.inputElement.click()`।",
  "Axios/fetch `componentDidMount`-এ; ডেটা এলে setState।",
  "render prop: `value` হিসেবে ফাংশন prop যা React element return করে—কোড শেয়ারিং।",
  "`componentDidMount`-এ `dispatch(fetchData())`; render-এ loading state দেখান।",
  "`connect(mapStateToProps, mapDispatchToProps)(App)`—state props-এ, actions bind।",
  "`@connect(...)` decorator syntax—experimental; babel plugin লাগে।",
  "`npx create-react-app my-app --template typescript` (react-scripts 3.3+); পুরনোতে `--typescript` বা `react-scripts-ts`।",
  "না—`statics` শুধু createClass-এ; ES6-এ `static propTypes`, class বাইরে `Component.x = ...`।",
  "inline callback ref প্রতি render-এ নতুন ফাংশন—null তারপর element দুবার কল। class field method stable রাখুন।",
  "Props Proxy (wrapped-এ props pass) ও Inheritance Inversion (Enhancer extends Wrapped)।",
  "state = {}, arrow handlers—constructor/bind ছাড়াই concise class components।",
  "event handler render phase-এর বাইরে; error boundary লাগে না—try/catch ব্যবহার করুন।",
  "try/catch imperative কোডে; error boundary declarative render/lifecycle error-এর জন্য।",
  "শুধু `render()` বাধ্যতামূলক।",
  "elements, array/fragment, portal, string/number, boolean/null (কিছু render নয়)।",
  "state initialize ও event handler bind (constructor-এ setState নয়)।",
  "না—state/handlers না থাকলে constructor লাগবে না।",
  "unmount-পর আর mount হবে না—setState অর্থহীন।",
  "error থেকে state আপডেট করে fallback UI (`return { hasError: true }`)।",
  "getDerivedStateFromProps → shouldComponentUpdate → render → getSnapshotBeforeUpdate → componentDidUpdate।",
  "getDerivedStateFromError, componentDidCatch।",
  "`ReactDOM.unmountComponentAtNode(container)`—DOM থেকে সরায়, listeners/state cleanup।",
  "render-এ HOC তৈরি করবেন না; static methods copy; refs `forwardRef` দরকার।",
  "forwardRef render function-এ নাম/displayName দিন DevTools-এ স্পষ্টতার জন্য।",
  "হ্যাঁ, প্যারামিটার পাসে সহজ; প্রতি render নতুন ফাংশন—পারফরম্যান্স খেয়াল।",
  "setState shallow merge—`setState({ comments })` posts অপরিবর্তিত থাকতে পারে।",
  "arrow `(e) => this.update(id, e)` বা `bind(this, id)`—synthetic event দ্বিতীয় আর্গুমেন্ট।",
  "শর্তে `return null` (যেমন loggedIn না হলে Greeting না দেখানো)।",
  "ThemeContext.Provider value পাঠান; ThemedButton `static contextType` বা Consumer দিয়ে পড়ে।",
  "class-এ `MyClass.contextType = MyContext` বা `static contextType = MyContext`।",
  "`<MyContext.Consumer>{value => ...}</MyContext.Consumer>`—function child।",
  "Provider `value`-কে state-এ রাখুন—inline `{ }` object প্রতি render নতুন reference।",
  "HOC refs pass করে না—`React.forwardRef` + `forwardedRef` prop pattern।",
  "শুধু `React.forwardRef` কম্পোনেন্টে দ্বিতীয় ref argument।",
  "forwardRef API পরিবর্তন breaking change—library-তে major version।",
  "`create-react-class`: getDefaultProps, getInitialState; auto-binding।",
  "হ্যাঁ—JSX `React.createElement`-এর syntactic sugar।",
  "render prop দিয়ে `<Mouse render={mouse => <Component mouse={mouse} />} />` HOC-এর মতো।",
  "create-react-app-এর scripts—dev server, HMR, build, test।",
  "React/JSX/TS, autoprefix CSS, dev server, tests, production bundle, PWA optional।",
  "SSR: `ReactDOMServer.renderToNodeStream`—HTML stream; শুধু server।",
  "`npx create-react-app my-app --template redux` বা `redux-typescript`।",
  "setState ছাড়া state মিউটেট করলে React জানে না—সবসময় immutable update + setState; Immutable.js optional।",
];

const md = fs.readFileSync(path.join(__dirname, "sudheer-react-en.md"), "utf8");
const oldStart = md.indexOf("## Old Q&A");
const oldEnd = md.indexOf("## Disclaimer");
const enMap = parseSudheerOldEnglish(md.slice(oldStart, oldEnd));

if (qBn.length !== 89 || aBn.length !== 89) {
  throw new Error(`Expected 89 BN entries, got q=${qBn.length} a=${aBn.length}`);
}

const oldQaBn = [];
for (let i = 1; i <= 89; i++) {
  const en = enMap.get(319 + i);
  if (!en) console.warn(`Missing English Old Q&A #${i}`);
  oldQaBn.push({
    q: qBn[i - 1],
    a: aBn[i - 1],
  });
}

const outPath = path.join(__dirname, "old-qa-bn-data.mjs");
const code = `/** Bengali Old Q&A (89). Regenerate: node scripts/build-old-qa-bn-data.mjs */\nexport const oldQaBn = ${JSON.stringify(oldQaBn, null, 2)};\n`;
fs.writeFileSync(outPath, code, "utf8");
console.log(`Wrote ${oldQaBn.length} entries to ${outPath}`);
